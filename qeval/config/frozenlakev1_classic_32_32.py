CONFIG = dict(
    ENV="FrozenLake-v1",
    TYPE="classic",

    N_ITERATIONS=2000,
    COLLECT_STEPS=1,
    MAX_STEPS=80,
    INITIAL_COLLECT=5000,

    EVAL_RUNS=10,
    EVAL_EVERY=25,
    EVAL_THRESHOLD=0.949,

    BATCH_SIZE=64,
    RB_CAPACITY=20000,

    EPSILON_START=1.0,
    EPSILON_MIN=0.1,
    EPSILON_DECAY=0.9,
    DECAY_EVERY=50,

    TARGET_UPDATE=100,
    GAMMA=0.99,
    LRATE=0.001,
    LAYERS=[32, 32],
)
